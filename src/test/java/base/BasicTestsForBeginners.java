//package base;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

//public class BasicTestsForBeginners {}
   /* private WebDriver driver;
    public void setUp1(){
        System.setProperty("webdriver.chrome.driver","resources/chromedriver.exe");
        driver= new ChromeDriver();
        driver.get("http://the-internet.herokuapp.com/");//launch the browser;
        //driver.manage().window().maximize(); //maximize the window
        //driver.manage().window().setSize(new Dimension(375, 812));//set the size wanted
        //System.out.println(driver.getTitle());//prints the title

        /*WebElement inputsLink = driver.findElement(By.linkText("Inputs"));
        inputsLink.click();*/

        /*List<WebElement> links = driver.findElements(By.tagName("a"));
        System.out.println(links.size()); //prints the number of links on the page
        driver.quit(); //quits the session */
    /*
    public static void main (String args[]){
        BaseTests test= new BaseTests();
        test.setUp();
     */
